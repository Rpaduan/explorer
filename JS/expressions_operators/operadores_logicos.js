// Operadores lógicos (logical operators)

// 2 valores booleanos, quando verificados, resultará em verdadeiro ou falso

let pao = true
let queijo = true

// AND && (e)
//console.log(pao && queijo)
// a resposta só é verdadeira se os dois forem verdadeiros, caso contrário será falsa

// OR || (ou)
//console.log( pao || queijo)
// a resposta é verdadeira sendo apenas um verdadeiro, só se torna falsa se os dois forem falsos

// NOT ! (não, negação)
console.log(!pao)
// ele troca o valor do objeto, se é verdadeiro, se torna falso, e vice-versa, como um botão liga-desliga