// Operadores de comparação

// Irá comparar valores e retornar um Boolean como resposta à comparação

let one = 1
let two = 2

/*
    // == igual a 
    console.log(two == 1)
    console.log(one == "1") // o == só avalia o valor, 1, e não o tipo, "1", para diferenciar o tipo usamos === (estritamente igual).

    // != diferente de 
    console.log(one != two)
    console.log(one != 1)
    console.log(one != "1") // o != só avalia o valor, 1, e não o tipo, "1", para diferenciar o tipo usamos !== (estritamente diferente)
*/

// > Maior que
    console.log(one > two)

// >= Maor igual a 
    console.log( one >= 1)
    console.log (two >= 1)

// Menor que
    console.log(one < two)

// <= Menor igual a 
    console.log(one <= two)
    console.log(one <= 1)
    console.log(one <= 0)